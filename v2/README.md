# v2 — active

Clean-slate rebuild. Pulls from v1 on demand — no engine abstraction until real usage demands it.

Design principles carried forward from the v1 critique:
- One primary unit, reshaped by phase (not stacked resources)
- Progressive reveal of hidden dimensions (don't show the full UI on turn 1)
- Mechanics replace rather than accumulate
- Exponent lives in production cadence, not cost
- Metaphor that generalizes across all 11 phases (not Enigma-specific)

Design notes and feedback continue to live in `/Users/gama/gemini/the-exponential-machine/` alongside v1's.
