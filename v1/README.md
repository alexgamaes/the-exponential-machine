# v1 — frozen

Original implementation (1939 Bletchley / Enigma-search framing, FLOPs + Data + Billets + Electricity, Phase 0–1 DAG).

Kept here for reference and as a donor for engine-style pieces (save harness, event log, theme toggle, simulator). Not under active development.

To play: open `v1/index.html` via the root `serve.py`.

To run v1 tests: `npm test` from the repo root (path already points here).
